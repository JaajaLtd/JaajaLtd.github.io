const setting = {
  setting: {
    selectedMenu: {},
    sideMenuTab: 'bio-data',
    showModal: false,
    showEditModal: false,
    showDeleteModal: false,
    showCreateModal: false,
    isToggled: false,
  },
};

export default setting;
